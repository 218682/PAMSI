#ifndef LISTA_HH
#define LISTA_HH
#include "tablica.hh"
class lista: public tablica{
private:
  tablica tab;
public:
  lista();
  void push(int);
  void remove(int);
  void add(int, int);
  void get (int);
};

#endif
